export const plaginOptionsManticore = {
    /*
    host: app.MHOST() || 'localhost',
    port: app.MPORT() || 9312,
    protocol: options.protocol || 'http',
    timeout: options.timeout || 10000
    */
}
